import django; django.setup(); from django.test import Client; from django.contrib.auth import get_user_model; client = Client(); User = get_user_model(); user = User.objects.filter(email="rubenbe9@gmail.com").first(); client.force_login(user); response = client.get("/admin/financing/payment/"); print(f"Status: {response.status_code}"); print(response.content.decode("utf-8")[:500] if response.status_code == 500 else "Success")
